<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
    <title>{% block title %}Pegasus Shop{% endblock %}</title>
</head>
<body>
    <nav class="apple-nav">
        <div class="nav-container">
            <div class="nav-logo">
                <a href="{{ url_for('cinema_index') }}">MCL Cinema</a>
            </div>
            <div class="nav-links">
                <a href="{{ url_for('cinema_index') }}">🎬 電影</a>
                <a href="{{ url_for('cinema_index') }}">購票</a>
                <a href="{{ url_for('index') }}">商店</a>
                {% if current_user.is_authenticated %}
                <a href="{{ url_for('profile') }}">個人</a>
                <a href="{{ url_for('logout') }}">登出</a>
                {% else %}
                <a href="{{ url_for('login') }}">登入</a>
                <a href="{{ url_for('register') }}">註冊</a>
                {% endif %}
                <div class="nav-search">🔍</div>
            </div>
        </div>
    </nav>
    
    <div class="container mt-4">
        {% with messages = get_flashed_messages(with_categories=true) %}
            {% if messages %}
                {% for category, message in messages %}
                <div class="alert alert-{% if category == 'error' %}danger{% elif category == 'success' %}success{% else %}info{% endif %} alert-dismissible fade show" role="alert">
                    {% if category == 'success' %}✅{% elif category == 'error' %}❌{% else %}ℹ️{% endif %}
                    {{ message }}
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                {% endfor %}
            {% endif %}
        {% endwith %}
        {% block content %}{% endblock %}
    </div>
    <footer class="footer">
        <div class="container">
            更多選購方式：<a href="#" style="color: #0066cc;">尋找你附近的 Apple Store</a> 或 <a href="#" style="color: #0066cc;">其他經銷商</a>。或致電 0800-020-021。
            <br><br>
            Copyright © 2025 Apple Inc. 保留一切權利。
        </div>
    </footer>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    
</body>
</html>